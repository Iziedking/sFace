export function renderRelayRules(onBack: () => void): HTMLElement { return simpleScreen('HOW IT WORKS', 'Steer with touch, arrow keys, or A and D. Carry up to three nodes, avoid hazards, and cross a gate to bank them.', 'Back to today', onBack); }
function simpleScreen(kickerText: string, copyText: string, actionText: string, action: () => void): HTMLElement { const section = document.createElement('section'); section.className = 'relay-screen'; const kicker = document.createElement('p'); kicker.className = 'relay-kicker'; kicker.textContent = kickerText; const heading = document.createElement('h1'); heading.textContent = 'Skill makes the difference.'; const copy = document.createElement('p'); copy.className = 'relay-lead'; copy.textContent = copyText; const button = document.createElement('button'); button.className = 'relay-primary'; button.type = 'button'; button.textContent = actionText; button.addEventListener('click', action); section.append(kicker, heading, copy, button); return section; }
