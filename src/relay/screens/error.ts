export function renderRelayError(messageText: string, onRetry: () => void): HTMLElement { const section = document.createElement('section'); section.className = 'relay-screen'; const heading = document.createElement('h1'); heading.textContent = 'Relay is offline.'; const copy = document.createElement('p'); copy.className = 'relay-lead'; copy.textContent = messageText; const button = document.createElement('button'); button.className = 'relay-primary'; button.type = 'button'; button.textContent = 'Try again'; button.addEventListener('click', onRetry); section.append(heading, copy, button); return section; }
